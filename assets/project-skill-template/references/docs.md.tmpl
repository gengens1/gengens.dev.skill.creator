# Private docs ({{DOCS_ROOT}})

根目录 `{{DOCS_ROOT}}/` 已 gitignore，禁止提交。

| 子目录 | 用途 |
|---|---|
| `task/` | 任务文档；{{DOCS_TASK_NOTE}} |
| `tmp/` | AI 临时输出 |
| `resource/` | 原始资料 |
| `sql/` | 私人 SQL：`YYMMDD-模块-作用.sql` |

开新任务：序号 = 该目录下现有最大号 + 1。公有文档仍写仓库 `docs/` 等（若项目有）。
